<%if activity.users.empty?%>
  <%unless params['past'] == 'true'%>
    No one yet, be the first!
  <%end%>
<%else%>
  <%=activity.users.map(&:account).flatten.map(&:name_with_initial).map{|name| name == current_user.account.name_with_initial ? "You" : name}.join(", ")%>
<%end%>
