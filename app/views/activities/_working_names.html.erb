<%if activity.users.empty?%>
  <%unless params['past'] == 'true'%>
    No one yet, be the first!
  <%end%>
<%else%>
  <%=activity.users.map(&:account).flatten.map(&:full_name).map{|name| name == current_user.account.full_name ? "You" : name}.join(", ")%>
<%end%>
